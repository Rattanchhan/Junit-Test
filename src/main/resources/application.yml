spring:
  application:
    name: Caching-Demo

  hazelcast:
    cluster-name: dev
    member-lists: localhost:5701

  datasource:
    url: jdbc:mysql://localhost:3306/caching_db
    username: root
    password: R@mySql@0900
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
server:
    port: 8081

